
# HOW TO run the package

This package can be launched using the `run.launch` package. *It has no options*: the package should be run entirely every time.

```bash
roslaunch robocluedo_rosplan run.launch
```
